# 🧪 TLSF 交互模拟器

> 动手体验 TLSF 的每一步：**分配 → 查找位图 → 分割 → trim → 释放 → 合并**。
> 开启「单步模式」，点分配/释放，用「下一步」逐步看算法执行，每步有解释。
> 算法忠实翻译自 `External/Allocator/tlsf/tlsf.c`（SL=32 槽、FL=25 类、8B 对齐、256B 小块阈值）。

<script setup>
import TlsfSimulator from './components/TlsfSimulator.vue'
</script>

## 这个模拟器在帮你回答什么问题

前面 [TLSF 算法那篇](./tlsf) 讲的是"两级位图怎么做到 O(1)"。
但算法讲完，脑子里往往还是空的——**因为它是动态过程**：位图什么时候亮、块什么时候被切开、
释放时哪两块合到一起，这些在静态文字里看不出来。

这个模拟器把那个过程画出来。你不需要先读完算法篇也能玩：
先随手点几次「分配」，看内存条怎么被切开、上方位图怎么亮起；
有疑问了再回头翻算法篇对应的小节。**先看现象，再看原理，比反过来容易得多。**

<TlsfSimulator />

## 术语先对齐

| 术语 | 一句话解释 |
|---|---|
| **TLSF** | 两级分割适应算法。用两层位图把"找空闲块"从遍历变成几次位运算 |
| **fl / sl** | 一级档号 / 二级槽号。两者合起来指向一个空闲链表 |
| **位图（bitmap）** | 一串 bit，每个 bit 表示"对应档位有没有空闲块" |
| **映射（mapping）** | 把请求尺寸换算成 (fl, sl) 的过程；`mapping_search` 会先向上取整 |
| **分割（split）** | 把一个空闲块切成"给用户的块 + 剩余块"，剩余块还回链表 |
| **trim** | 分割之后把多余尾部还给池子的那一步（`block_trim_free`） |
| **合并（merge）** | 释放时把相邻空闲块合起来，前向/后向各一次机会 |
| **块头（block header）** | 每块前面的元数据：size 字段 + 状态位；空闲时还兼作链表指针 |
| **单步模式** | 模拟器的开关：打开后每次操作都要点「下一步」，能看到中间状态 |

## 使用建议（按学习顺序）

1. **只看不动**：先点几次「分配」（如 64B/128B/32B），关单步模式直接完成，观察内存条上块如何分割、位图如何亮起
2. **单步学习**：开启「单步模式」分配一块 64B —— 你会依次看到：映射计算 → 位图查找 → 命中块 → 分割 → 剩余入池
3. **重点看释放**：分配两块相邻的内存再释放其中一块，观察 **block_merge_prev / block_merge_next** 的合并动画（两块合一大块）
4. **理解碎片**：连续分配 64B×6，释放第 2、4、6 块，再尝试分配 128B —— 观察外部碎片如何导致"有空闲但不够大"
5. **对照源码**：每步解释都对应 `tlsf.c` 的具体函数（mapping_search / search_suitable_block / block_split / block_merge_prev）

## 对照表：模拟器 ↔ 源码

| 模拟器步骤 | 源码函数 | 说明 |
|---|---|---|
| 映射计算 | `mapping_insert` / `mapping_search` (tlsf.c:508/528) | size → (fl, sl) 槽 |
| 位图查找 | `search_suitable_block` (tlsf.c:538) | sl_bitmap → fl_bitmap → ffs |
| 命中摘除 | `remove_free_block` (tlsf.c:571) | 双向链表摘除 + 位图维护 |
| 分割 | `block_split` (tlsf.c:642) | 切出请求大小，剩余为新空闲块 |
| 尾部回收 | `block_trim_free` (tlsf.c:705) | 分配后多余尾部还给池 |
| 释放合并 | `block_merge_prev` / `block_merge_next` (tlsf.c:674/689) | 前后相邻空闲块合并 |
| 插入链表 | `insert_free_block` (tlsf.c:600) | 头插 + 置位图 |

## 局限说明（教学简化）

- 模拟器用**物理块列表** + 每槽链表头表示空闲链表，真实 TLSF 是完整双向链表
- 内存条仅 512B（真实 Unity 用 256MB 虚拟预留 + 多 Pool），便于观察
- 块头开销（prev_phys_block + size 状态位）未逐字节模拟，聚焦算法流程

## 自检清单

玩完之后，你应该能回答：

- [ ] 分配 64B 时，位图上亮起的是哪一位？为什么是那一位（口算一遍 `mapping_insert`）？
- [ ] 「本档没有块」时模拟器做了什么？对应源码里哪两个 bitmap 在配合？
- [ ] 为什么切完块之后，剩下的那部分要"还回池里"而不是留着浪费？
- [ ] 什么情况下模拟器不给切、直接把整块交出去？这和真实源码的哪个判据一致？
- [ ] 释放时前后合并靠什么判断"邻居是不是空闲的"？
- [ ] 第 4 条实验里为什么"有空闲却分配不出 128B"？这是内部碎片还是外部碎片？
- [ ] 把模拟器里的 512B 换成真实 Unity 的 256MB 预留，哪些结论会变、哪些不变？

## 延伸阅读

- [TLSF：两级分割适应算法](./tlsf) — 算法本身的逐行拆解（碰到看不懂的步骤回这里查）
- [DynamicHeapAllocator：TLSF 的工程集成](./dynamic-heap) — 真实工程里 TLSF 外面还包了什么
- 🧪 [一次分配的完整旅程](./allocator-journey) — 从 `UNITY_NEW` 一路走到这里的单步演示
