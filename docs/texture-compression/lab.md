# 纹理压缩实验台（建设中）

> 上传一张图，逐步看它被 ETC1 / BC1 / ASTC / BC7 压缩的**全过程**：每步数据怎么被处理、每步后的效果、64bit/128bit 怎么排、每个块解码前后差多少。

## 路线图

- [ ] 上传图片 → 自动缩放/裁剪到块对齐尺寸（100×100 等）
- [ ] 六种格式同图压缩对比：ETC1 / BC1（真实编码器）｜ ASTC 4×4/5×5/6×6（教学合法 bitstream）｜ BC7（教学简化）
- [ ] 全流程逐步日志：切块 → 逐块编码（子块均值 → 模式 → 修正表 → 索引 → bit 打包）→ 解码对比
- [ ] 点击任意块 → 该块 7 步下钻 + 前后像素对照
- [ ] 输出：文件大小 / bpp / PSNR / 模式分布统计卡

引擎（`textureEncoders.js`）已完成并通过单测：ETC1/BC1 真实 bitstream（与 Mesa 口径解码对拍）、ASTC 教学编码器三 footprint 均可被官方 astcenc 解码（合法非最优）。UI 组件建设中。

板块：[总览](./) → [ETC 单步](./etc-encode-100x100) → [ASTC 单步](./astc-encode-100x100) → [DXT/BC](./dxt-bc1-7) → [纹理压缩实验台](./lab)
